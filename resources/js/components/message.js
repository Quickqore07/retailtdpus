/**
 * Message component for backward compatibility
 * Re-exports the messageService for use in mixins and route guards
 */

import { messageService } from '../plugins/message'

export default messageService

