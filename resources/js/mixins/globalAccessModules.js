/**
 * Global Access Modules
 * List of resources that have global access (not restricted by business unit)
 */
export const globalAccessModules = [
    // Add your global access module names here
    // Example: 'users', 'settings', 'roles'
];

